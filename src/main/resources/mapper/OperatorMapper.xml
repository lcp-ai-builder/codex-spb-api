<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lcp.spb.logic.dao.OperatorMapper">
  <resultMap id="OperatorResultMap" type="com.lcp.spb.bean.Operator">
    <id property="id" column="id" />
    <result property="operatorNo" column="operator_no" />
    <result property="name" column="name" />
    <result property="loginName" column="login_name" />
    <result property="loginPassword" column="login_password" />
    <result property="phone" column="phone" />
    <result property="email" column="email" />
    <result property="roleId" column="role_id" />
    <result property="status" column="status" />
    <result property="createdAt" column="created_at" />
    <result property="lastLoginAt" column="last_login_at" />
  </resultMap>
  <sql id="Base_Column_List">
        id, operator_no, name, login_name, login_password, phone, email, role_id, status, created_at, last_login_at
  </sql>
  <select id="findPage" resultMap="OperatorResultMap">
    select
    <include refid="Base_Column_List" />
    from operators
    order by id
    limit #{limit} offset #{offset}
  </select>
  <select id="countAll" resultType="long">
    select count(id) from operators
  </select>
</mapper>